<style>
    * {
      box-sizing: border-box;
    }

    body {
      background-color: #f4f4f4;
      padding-top: 10vh; /* padding for navbar */
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      min-height: 100vh; /* Make body at least height of viewport */
    }

    #self-desc {
      max-width: 80%;
      max-height: 90vh;
      margin: 0 auto;
      background-color: #ffffff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
      padding: 20px;
      display: flex;
      flex-direction: row;
    }

    .text {
      flex: 1;
      padding-right: 20px;
      font-size: 18px; /* Adjust font size as needed */
    }

    .zodiac {
      max-height: 420px;
      flex: 1;
      
    }

    .zodiac img {
      max-width: 100%;
      max-height: 100%;
      border-radius: 10px;
    }
    
    .btn {
      border: none;
      padding: 10px 20px;
      cursor: pointer;
      border-radius: 5px;
      color: #fff;
    }

    .btn-export {
      background-color: #1DB954; /* Green background color */
    }

    .btn-copy{
      background-color: #5e5e5e;
      border-style:double;
      border-color: black;
    }
    .btn-download{
      background-color: #1DB954;
    }

    /* Modal styles */
    .modal {
      display: none;
      position: fixed;
      z-index: 1;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      overflow: auto;
      background-color: rgba(0, 0, 0, 0.5);
    }

    .modal-content {
      background-color: #fefefe;
      margin: 15% auto;
      padding: 20px;
      border: 1px solid #888;
      width: 80%;
      max-width: 500px;
      overflow:hidden;
    }

    .close {
      color: #aaa;
      float: right;
      font-size: 28px;
      font-weight: bold;
    }

    .close:hover,
    .close:focus {
      color: black;
      text-decoration: none;
      cursor: pointer;
    }
</style>

<div id="self-desc">
    <div class="text">
        <h1>{{ zodiac }}</h1>
        <h2>What your music says about you!</h2>
        <p>We've used a state-of-the-art algorithm to analyze your musical data and assign you your very own personal musical horoscope.</p>
        <p>{{ zodiacDescription }}</p>        
        <button type="button" class="btn btn-export" id="exportBtn">Share</button>
          <div id="popup" class="modal">
            <div class="modal-content">
              <span id="closeBtn" class="close">&times;</span>
              <h2>Preview</h2>

              {{!--Get zodiac graphic based on zodiac algorithm--}}
              <img class="card-img-top" id="zodiacImage" src="{{zodiacImagePath}}">
              {{!--Get zodiac sign name based on zodiac algorithm--}}
              <button type="button" class="btn btn-copy" id="copyBtn">Copy Link</button>
              <button type="button" class="btn btn-download" id="downloadBtn" href="{{zodiacImagePath}}" download>Download Zodiac Graphic</button>
            </div>
          </div>
        <script>
          var publicZodiacImagePath = "{{publicZodiacImagePath}}";
        </script>
        <script src="../../resources/js/script.js"></script>
    </div>
    <div class="zodiac">
      <img id="zodiacImage" src="{{ zodiacImagePath }}" alt="Zodiac Image">
  </div>
</div>